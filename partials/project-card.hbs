<article class="project-card">
  {{#if feature_image}}
    <a href="{{url}}"><img src="{{feature_image}}" alt="{{title}}"></a>
  {{/if}}
  <h2 class="text-2xl font-playfair mt-4"><a href="{{url}}">{{title}}</a></h2>
  <p class="text-silver-highlight">{{excerpt}}</p>
</article>
